# Copied from GitLab.
The main idea of the project is to implement SQL queries inside Pandas. The tasks gradually progress from simple data selection from SQLite through subqueries, table joins, and aggregations to a full-fledged A/B test with calculating the delta between the first commit and the deadline, dividing into test and control groups, and calculating the correlation.
